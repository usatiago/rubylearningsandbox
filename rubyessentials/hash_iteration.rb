#!/usr/bin/ruby

business = {"name"=>"Treehouse","location"=>"Portland,OR"}
business.each do |key, value|
    puts " the hash key is #{key} and the hash value is #{value}"
end

business.each do |key, value|
    puts " the hash key is #{key} and the hash value is #{value}"
end